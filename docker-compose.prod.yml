services:
  tcflask:
    image: t0mc4mp/tom.camp:latest
    env_file:
      - ./prod.env
    volumes: []
    command: >
      uv run flask run
      --host 0.0.0.0
      --port 5000
    networks:
      - app-network

networks:
  app-network:
    external: true
    name: shared-db-network
